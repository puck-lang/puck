///// Attributes are only supported on enum, trait and type declarations
#[attribute]
fn func() {}

///// Attributes can only contain simple string literals. Interpolation is not supported.
let a = 1
#[attribute = '$a']
type Type {}
